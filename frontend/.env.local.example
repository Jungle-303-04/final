# Local FastAPI gateway. Its routes are mounted at the origin root.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
VITE_BACKEND_API_PREFIX=

# The UI lab uses live contracts by default. Set fixture only for offline work.
VITE_DEVPREVIEW_DATA_MODE=live
