# 데모 결함 원장 — 전수 감사(2026-07-19) 잔여분

전수 소스 감사(9파일)에서 나온 결함 중 **이번 커밋에서 해소된 것**과 **의도적으로 남긴 것**의 단일 장부.
남긴 항목은 전부 사유가 있으며, 무단 방치가 아니다. 해소 시 이 파일에서 지운다.

## 해소됨 (이번 커밋)
- [블로커] 흐름 그래프 상태·복제 수를 단일 인벤토리에서 파생(지도·벨과 장애 일치, worker 노드 추가)
- [블로커] AI 기본 대화의 유령 파드 checkout-104 → 실제 장애 파드 redis-605로 전면 교체
- AI 스크립트 수치(파드 37/위험 1) → 인벤토리 사실(파드 78/장애 3)
- W7 노드 7대 → `nodes.length` 파생 / W8 복제 6→8 문구 → 파드 수 파생
- 상세 전략 Recreate 반전 → RollingUpdate / SA 파생 2원화 → 단일화
- Node 상세 Phase가 "Running"으로 위장 → 종류별 상태 필드 우선
- 파드 행 호버의 죽은 버튼 3종(로그/이벤트/재시작) 제거 → "상세" 표시로
- 표 헤더 가짜 정렬 셰브론 제거 / AI 새 대화·수정 죽은 버튼 제거
- AI 근거·링크 href="#" → 버스(open_ref/open_crit)로 셸 상세·목록 실이동
- Esc 층위 규칙(팝오버→모달→상세→AI 한 겹씩) / 토폴로지 드래그 릴리즈 오클릭 방지
- 맵·토폴로지 툴팁 PRESENT_SCALE 좌표 보정 / 토폴로지 조작 설명 카피 제거
- 어휘 통일: 범례·카드·위젯 장애/주의 (임계·경고 혼용 제거)
- 연결 위저드 유령 클러스터(cluster-1/2/3) → prod-eks/dev-eks
- 임베드 장애 칩 클릭이 내부 미니 상세를 여는 이원화 → 셸 오버레이로 일원화
- 로그아웃이 보드 배치(localStorage)를 남기던 것 → 초기화 포함
- 자동 갱신 옆 버튼처럼 보이는 RotateCw 제거 / 검색 placeholder 설명 제거 / W8 시간 칸 52px

## 남김 (사유 명기 — 제품 이식(P2~P5)에서 해소가 더 싼 구조 변경)
- 요약 칩 줄이 홈·지도에 두 벌(시각·표기는 통일됨, 렌즈 인터랙션 차이만) → 공용 `<StatusSummaryRow>` 추출은 P2 공용 부품에서
- 워크로드 행의 클러스터 귀속이 이름 해시(파드는 노드 기준) → 드릴 스코프에서 워크로드 표가 이상해질 수 있음. 인벤토리 귀속 통일은 어댑터 재작업 — P3 `clusterStats` 어댑터에서
- 파드 age·image 파생 2원화(맵 vs 표) / 섹션 제목 카운트(환경 변수 12 등) 불일치 / READY 2/2인데 UTD 1 / 노드 스펙 상한 고정 4000m — 전부 인벤토리 파생 확장으로 P3
- Service 표에 shop-web·redis·worker 행 부재(맵 렌즈엔 존재) / 벨 이벤트 상대시각 W8과 불일치 — 데이터 스펙 확장 P3
- YAML replicas가 행 READY와 무관 / Esc가 YAML 편집 초안을 무경고 폐기 — 상세 시트 재작성(P4)에서
- 벨 repo 카드가 클릭 불가인데 클릭형과 동일 외형 / AI 대화 목록 행 무동작 / AI 스크롤 effect deps — 소품질, P4·P5
- 벨 알림의 상대시각이 타임라인 서피스와 개별 생성(타임라인·W8은 `timelineItems()` 단일화 완료) — P4 벨 통합 시 동일 소스로

## 추가 해소 (요약 계층 커밋)
- 서피스 요약 칩(배포·인시던트), 점검 통과 정책 근거 목록(카탈로그 24종), 벨 '진행 중/최근' 2단, 파드 상세 RingGauge(P-31)
- W3 제목을 '저장소 동기화'로 명시 — 배포 화면의 앱 단위 수치와 단위 혼동 차단

## 추가 해소 (서피스 신설 커밋)
- 6개 서피스(배포·인시던트·타임라인·점검·비용·설정) 데모 구현 — 비활성 메뉴 해제, 전 정보 단일 인벤토리 파생
- WidgetFrame 딥링크(`>`) 배선 — 목적지 서피스가 생겨 전 위젯에 "전체 보기" 활성
- 비용 단일 소스 `costModel()`(노드 스펙 파생) — W7·/cost 동일 숫자 보장, W8·타임라인 `timelineItems()` 단일화
- 미사용 잔재: `Kind.view`, `belowContent`, `NAV_ITEMS.href`, ClusterRow의 `onKind`·`related`, theme의 TYPE/RADIUS/ELEV 미사용+TINT_G 중복 — 데모 동결 전 일괄 정리 커밋 1회로
