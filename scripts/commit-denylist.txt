# 커밋 제목에 남기지 않는 단어 (오픈소스 공개 대비)
#
# 한 줄에 하나. '#' 은 주석. 대소문자를 구분하지 않는다.
# scripts/commit-msg-gate.sh 가 이 목록을 강제한다.
#
# 왜: 외부 레퍼런스 구현체의 이름과 내부 기획 문서 ID는 커밋 제목에 남기지 않는다.
#     커밋 제목은 "무엇이 달라졌는가"를 말한다. "어디서 베꼈는가"나
#     "몇 번 문서를 따랐는가"는 코드·문서 안에 남기고, 제목에는 쓰지 않는다.
#     (레퍼런스 귀속은 NOTICE·파일 헤더에 남긴다 — 그게 라이선스가 요구하는 곳이다)

# 외부 레퍼런스 구현체
radar
skyhook

# 내부 기획 문서 ID (제목이 아니라 본문·문서에 쓴다)
# 정규식이므로 백슬래시를 쓰지 않는다 (읽는 쪽에서 이스케이프가 깨진다)
VP-[0-9]
BQ-[0-9]
GAP-[0-9]
D-[0-9][0-9][0-9]
