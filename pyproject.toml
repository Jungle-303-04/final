[project]
name = "project-k"
version = "0.1.0"
description = "FastAPI service for Project K"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.116.0",
    "prometheus-fastapi-instrumentator>=8.0.2",
    "psycopg[binary]>=3.3.4",
    "pydantic-settings>=2.10.0",
    "sqlmodel>=0.0.38",
    "uvicorn[standard]>=0.34.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.0",
    "pytest>=8.4.0",
    "ruff>=0.12.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
