# -----------------------------------------------------------------------
# WARNING: 아래 값들은 모두 플레이스홀더(placeholder)입니다.
# 운영 환경에서는 CI/CD 파이프라인 또는 SOPS 등을 통해 실제 값을 주입하세요.
# 이 파일을 Git에 커밋할 때 실제 비밀 값이 포함되지 않도록 주의하십시오.
# -----------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: management-runtime-secret
  namespace: management
type: Opaque
stringData:
  DATABASE_URL: "postgresql://kubeheal:changeme@postgresql:5432/kubeheal"
  NATS_URL: "nats://nats:4222"
  REDIS_URL: "redis://redis:6379/0"
  LLM_PROVIDER: ""
  LLM_MODEL: ""
  LLM_API_KEY: ""
  LLM_BASE_URL: ""
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
  GEMINI_API_KEY: ""
  GOOGLE_API_KEY: ""
  GITHUB_TOKEN_REF: "env:GITHUB_TOKEN"
  SMTP_HOST: ""
  SMTP_USERNAME: ""
  SMTP_PASSWORD: ""
  SMTP_FROM: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  namespace: management
type: Opaque
stringData:
  POSTGRES_USER: "kubeheal"
  POSTGRES_PASSWORD: "changeme"
  POSTGRES_DB: "kubeheal"
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: management
type: Opaque
stringData:
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "changeme"
