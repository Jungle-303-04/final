apiVersion: v1
kind: Service
metadata:
  name: final-api
  labels:
    app.kubernetes.io/name: final-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: final-api
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
