kind: Service
spec:
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    app: demo-game-gateway
metadata:
  name: login-gateway
  labels:
    app: demo-game-gateway
  namespace: sandbox
apiVersion: v1
