kind: Service
spec:
  ports:
  - name: http
    port: 8083
    targetPort: http
  selector:
    app: session-gateway
metadata:
  name: session-gateway
  labels:
    app: session-gateway
  namespace: sandbox
apiVersion: v1
