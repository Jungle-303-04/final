apiVersion: v1
kind: Service
metadata:
  name: final-postgres
  labels:
    app.kubernetes.io/name: final-postgres
spec:
  selector:
    app.kubernetes.io/name: final-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
