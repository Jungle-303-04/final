meta {
  name: 02 카탈로그 항목 상세
  type: http
  seq: 2
  tags: [catalog, read]
}

get {
  url: {{base_url}}catalog/items/{{catalog_item_id}}
}

tests {
  test("200 or auth/not-found", function () {
    expect([200, 401, 403, 404]).to.include(res.status);
  });
}
