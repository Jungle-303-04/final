meta {
  name: 03 조직 삭제
  type: http
  seq: 3
  tags: [
    management
    org
    admin
    write
    destructive
  ]
}

delete {
  url: {{base_url}}orgs/{{created_org_id}}
}

tests {
  test("delete is explicit success/auth/conflict", function () {
    expect([204, 401, 403, 404, 409, 422]).to.include(res.status);
  });
}
