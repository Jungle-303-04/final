meta {
  name: 12 권한 회수
  type: http
  seq: 12
  tags: [
    management
    access
    admin
    write
    destructive
  ]
}

delete {
  url: {{base_url}}access/{{access_id}}
}

tests {
  test("delete is explicit success/auth/not-found", function () {
    expect([204, 401, 403, 404, 422]).to.include(res.status);
  });
}
