meta {
  name: 03 복구 액션 선택(실행)
  type: http
  seq: 3
  tags: [rca, recovery, write, deploy]
}

post {
  url: {{base_url}}rca/recovery-plans/{{plan_id}}/actions/{{action_id}}/select
}

headers {
  content-type: application/json
}

body:json {
  {
    "reason": "bruno operator selection"
  }
}

tests {
  test("200 or auth/validation/not-found", function () {
    expect([200, 401, 403, 404, 409, 422]).to.include(res.status);
  });
}
