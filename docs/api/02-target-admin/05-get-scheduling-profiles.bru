meta {
  name: 05 Scheduling profile 조회
  type: http
  seq: 5
  tags: [
    target
    scheduling
  ]
}

get {
  url: {{base_url}}clusters/{{agent_cluster_id}}/scheduling-profiles
  body: none
}

tests {
  test("read success or explicit auth response", function () {
    expect([200, 401, 403, 404]).to.include(res.status);
  });
}
