meta {
  name: 03 알림 채널 삭제
  type: http
  seq: 3
  tags: [
    alert
    admin
    routing
    destructive
  ]
}

delete {
  url: {{base_url}}alert-channels/{{alert_channel_id}}
}

tests {
  test("delete is explicit success/auth/not-found", function () {
    expect([204, 401, 403, 404, 422]).to.include(res.status);
  });
}
