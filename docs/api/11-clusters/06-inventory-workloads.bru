meta {
  name: 06 인벤토리 workloads
  type: http
  seq: 6
  tags: [clusters, inventory, read]
}

get {
  url: {{base_url}}clusters/{{cluster_id}}/inventory/workloads
}

tests {
  test("200 or auth/not-found", function () {
    expect([200, 401, 403, 404]).to.include(res.status);
  });
}
