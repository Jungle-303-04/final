meta {
  name: 10 디플로이먼트 재시작(sandbox)
  type: http
  seq: 10
  tags: [clusters, action, write, deploy]
}

post {
  url: {{base_url}}clusters/{{cluster_id}}/namespaces/{{namespace}}/deployments/{{deployment_name}}/restart
}

headers {
  content-type: application/json
}

body:json {
  {
    "reason": "bruno restart test"
  }
}

tests {
  test("200 or auth/validation/not-found", function () {
    expect([200, 401, 403, 404, 409, 422]).to.include(res.status);
  });
}
