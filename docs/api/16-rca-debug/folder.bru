meta {
  name: 16 RCA 실제 E2E 워크플로우
  type: folder
  seq: 16
}

script:pre-request {
  const enabled = String(
    bru.getEnvVar("rca_test_verification") || bru.getVar("rca_test_verification") || "false"
  ).trim().toLowerCase() === "true";
  if (!enabled) {
    bru.runner.skipRequest();
  }
}
