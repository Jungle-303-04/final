# 데모 준비 상태

마지막 실측: 2026-07-15 04:42 KST

`통과`는 실제 시스템을 화면에서 끝까지 확인한 경우만 뜻한다. 일부 환경에서만 확인했거나
아직 실행하지 않은 동선은 `부분 통과` 또는 `미검증`으로 구분한다.

| 동선 | 상태 | 실측 소요 시간 | 근거·막힌 이유 |
|---|---|---:|---|
| 데모① 클러스터 연결 → 노드·파드 확인 | 미검증 | — | 위자드로 새 게임 클러스터를 처음부터 연결하는 전체 동선 미실행 |
| 데모② 게임 요청 → 실시간 로그 | 부분 통과 | 평균 2.372초 | 기존 게임의 실제 로그 도착은 확인. 새 COLOR-TURF-ARENA 배포에서는 미검증 |
| 데모③ 실제 부하 → 알림 → HPA 확장 | 미검증 | — | 봇 수별 CPU, 알림 발생, HPA 확장, 새 파드 화면 반영을 한 번에 측정하지 않음 |
| 데모④ 실제 OOM → RCA → 복구 승인 → Ready | 통과 | 81초 | 19:34:59Z 주입, 79초에 화면에서 선택 접수, 19:36:20Z 새 ReplicaSet 파드 Ready. 목표 85초 이내 |
| 데모⑤ 변경 이력 → Git 기록 → 되돌리기 | 미검증 | — | 발표 동선 전체 미실행 |
| D5 과거 메트릭 리플레이 | 실패 | 관측 간격 중앙값 30.231초 | 공개 `/api/metrics/history` 최근 15분 응답 31점, 간격 24.239~30.690초. 1초 증거 해상도 미충족이므로 대본에서 제외 |

## 화면 근거

- 데모④ 선택 접수: `/tmp/opsia-d1-selection-received.png`
- 데모④ 복구된 파드: `/tmp/opsia-d1-recovered-pod.png`

스크린샷은 로컬 실측 증거이며 저장소에는 포함하지 않는다.
