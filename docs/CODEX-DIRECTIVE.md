# CODEX 실행 지시서 — 목표 모드 (Goal Mode)

> 발행: 2026-07-19 · 발행자: 우녕(오케스트레이터 세션 경유)
> **마감: 2026-07-19 (토) 13:00 KST — 무감독 야간 실행.** 발행자는 자러 간다. 사람에게 질문하지 말 것 — 판단이 필요한 지점은 참조 문서의 결정을 따르고, 문서에 없으면 보수적 선택 후 `docs/BLOCKERS.md`에 기록하고 전진한다.
> 성격: **목표 달성형 명령서.** 각 목표(G#)는 측정 가능한 완료 조건을 갖고, 완료 조건 충족 → 자동으로 다음 목표 진행. 막히면 우회하지 말고 원인을 `docs/BLOCKERS.md`에 기록 후 해소가 선행 목표가 된다.

## 마감 우선순위 (13:00 판정 기준)

| 등급 | 13:00까지 | 목표 |
|---|---|---|
| **MUST — 하나라도 미완이면 실패** | 배포 1회 완주(G0) · 브랜치 위생(G1) · 파운데이션(G2) · G4 중 홈/리소스 3관점/알림 단일화/연결 모달 | G0·G1·G2·G4 핵심 |
| **SHOULD** | G4 나머지 서피스(배포·인시던트·타임라인·점검·비용·설정) · G5 제거 목록 | G4 완성·G5 |
| **COULD** | G6 폴리싱·E2E 녹화 · G7 리듬 | G6·G7 |

권장 시간 배분(6h): G0 병렬 시작+감시 0.5h · G2 1.5h · G4 핵심 2.5h · G5 1h · 검증·로그 0.5h. 미완 목표는 상태·잔여량·다음 손을 GOAL-LOG에 남긴다(침묵 미완 금지).

**13:00 이후 = 폴리싱 무한 루프.** 작업은 절대 멈추지 않는다 — G6의 판매 품질 스윕(모순 0·중복 0·가짜 컨트롤 0·자연스러움)을 E가 반복 실행하고, 발견 결함을 트랙에 반송·수정·재검을 무한 반복한다. "할 일 없음" 상태는 존재하지 않는다: 결함 0이면 원장·스펙의 SHOULD/COULD 잔여를 소거한다.

**품질 기준 3행(모든 판단의 잣대)**: ① 판매 가능한 프로덕션 품질 ② 모든 정보가 누락 없이 **일관된 인터페이스**로 제공 ③ 자연스러움(레퍼런스 관례를 벗어난 배치·표현 금지). **쓸데없는 테스트 금지** — 테스트는 계약(스키마·발화 매트릭스·숫자 정합)과 회귀 방지 목적만 작성하고, 구현 세부를 반복 검증하는 스냅샷·형식 테스트는 만들지 않는다.
> 참조 계층(충돌 시 위가 우선): ① `HANDOVER.md` §1 불변 조건 ② 본 지시서 ③ `frontend/OPSIA-MASTER-SPEC.md`(결정) ④ `docs/PRODUCTION-READINESS-PLAN.md`(무중단 절차) ⑤ `frontend/OPSIA-SURFACE-SPEC.md`·`frontend/DEMO-DEFECT-LEDGER.md`(요소 바인딩·잔여 결함)

---

## 0. 절대 운영 규칙 (모든 목표·모든 에이전트 공통 — 위반 즉시 중단)

1. **배포 창(Deploy Window) 규칙 — 이번에 확정된 병목의 해소책.** Dev Deploy 실행이 시작되면 **완주(성공/실패)까지 dev 푸시 금지, 실행 취소 절대 금지.** 지금까지 배포가 한 번도 완주 못 한 원인은 코드가 아니라 "새 반복을 위해 실행 중 배포를 취소"하는 루프였다(run 29662399433 취소 실측). 개발 커밋은 작업 브랜치에 쌓고, dev 푸시는 **배포 완주 후 배치로**.
2. push 전 `git fetch origin dev` — 타인 커밋 reset/revert 금지, force-push 금지.
3. 데모(`demo/motion-animations`, 태그 `demo-freeze-v2`)는 **읽기 전용 견본**이다. 수정 금지, 코드 복사 금지 — 사양을 보고 제품 규칙(토큰·i18n·가드)으로 재구현한다.
4. 커밋: 한국어 conventional(`<type>: 명사 키워드 / 명사 키워드`), author `choi woo-nyong <woonyong.kr@gmail.com>`, 페이즈=커밋 단위.
5. 게이트: 모든 커밋은 `make gate-fast` 통과 후 푸시(로컬 훅 실패 시 원인은 대개 conda 셸의 volta PATH 가림 — `conda deactivate` 후 재시도, 코드 탓하지 말 것).
6. 완료 선언 금지 조항: "버그 0" 선언으로 목표를 닫지 않는다 — 자동 테스트·API 계약·라이브 상태·실브라우저 E2E가 **같은 digest**를 가리킬 때만 닫는다(HANDOVER §11).

## 0b. 서브에이전트 분업 구조 (최대 병렬화)

오케스트레이터(너)는 직접 코딩하지 말고 **트랙별 서브에이전트에 위임**하라. 파일 소유권이 겹치지 않게 분할하고, 트랙 간 의존은 목표 순서로만 해결한다.

| 트랙 | 역할 | 소유 영역 | 상시 지시 |
|---|---|---|---|
| **A. 파이프라인** | 배포 완주·CI 안정화 | `.github/workflows`, `scripts/*deploy*`, `Makefile` | 배포 창 규칙의 집행자. 배포 상태를 `docs/BLOCKERS.md`에 기록 |
| **B. 파운데이션** | 토큰·공용 부품 (P1~P2) | `src/styles/`, `src/motion/`, `shared/ui/{table,status,brand,connect,charts,widgets}/` | 부품은 단 한 번 정의 — 두 번째 구현 발견 시 즉시 삭제 이슈화 |
| **C. 서피스** | 화면 이식 (P3~P5) | `src/pages/`, `src/app/`, `src/features/` | 데모 견본(5174)과 화면 대조 스크린샷을 커밋 메시지에 언급 |
| **D. 정리** | 중복·레거시 삭제, 브랜치 위생 | 삭제 전용(신규 코드 금지) | 삭제는 "결정표의 제거 목록"에 있는 것만. 목록 밖 삭제는 제안만 |
| **E. QA** | 상시 적대 검증 | 읽기 전용 + 테스트 파일 | 매 목표 완료 시 8.2 판매 품질 스윕(모순 0·중복 0·가짜 컨트롤 0) 실행, 결함은 트랙에 반송 |

병렬 규칙: B∥A 동시 시작 가능. C는 B의 해당 부품 완료 후. D는 C가 교체를 마친 대상만. E는 항상.

---

## G0. 배포 완주 (선행 목표 — 이것이 끝나기 전 다른 트랙은 dev 푸시 금지)

**완료 조건**: Dev Deploy 1회가 성공으로 완주하고, 라이브 digest가 새 SHA를 가리키며, post-deploy 스모크 통과.

- A: 현재 게이트 통과 SHA로 수동 발사 — `gh workflow run "Dev Deploy" -r dev -f source_sha=<게이트 성공 풀SHA> -f deployment_scope=FULL -f previous_release_sha=<현 라이브 SHA>`. 실행 중 어떤 이유로도 취소 금지. 실패 시 실패 스텝 로그를 BLOCKERS에 기록하고 그 해소가 G0 하위 목표.
- A: 완주 후 `docs/current-service-state.md` 재측정 갱신(HANDOVER §11 절차).

## G1. 브랜치 위생 — dev 단일 본선화

**완료 조건(발행자 확정 정책)**: 원격 브랜치 = **`main` + `dev` 둘만**. 그 외 전부(작업 브랜치·`demo/motion-animations` 포함) — ① 가치 있는 내용은 dev로 머지 ② `archive/<이름>` 태그로 아카이브 ③ 브랜치 삭제.

- D: 미병합 브랜치는 diff 검토 — **같은 기능이 두 벌이면 더 통일된 쪽(결정표 D# 준수 쪽)을 채택**하고 나머지는 삭제. 채택 근거를 커밋 메시지에 인용.
- D: 데모 브랜치는 견본 가치가 문서·태그(`demo-freeze-v2`)로 보존되므로, G4 서피스 이식이 MUST 범위를 넘긴 시점에 태그만 남기고 삭제한다(그 전에는 읽기 전용 참조로 유지).
- D: main은 "배포 완주 SHA의 승격 대상"으로 정의 — G0 완주 digest를 main에 fast-forward.

## G2. 파운데이션 (Master Spec P0~P2)

**완료 조건**: `check:design` 확장 게이트(모션 리터럴 검사 포함) 통과 상태에서 — 신설 토큰(+다크 값) 등록, 공용 부품 8종(`ResourceTable`·`StatusPill`·`TintChip`·`ConnectStages`·brand 아이콘·`MiniBar`류 차트 부품) 존재, `@tanstack/react-table`·`@tabler/icons-react` 의존성 제거.

- B: Master Spec 4장 매핑표 그대로. 데모 hex 복사 금지. 다크 값 누락 = 게이트 실패.
- B: D17 — 프리미티브 개폐 애니를 모션 토큰으로 재정의, `check:design`에 리터럴 duration grep 추가.
- 견본 참조: 데모 `devpreview/widgets.tsx`(부품 원본), `devpreview/theme.ts`(값 의미).

## G3. 백엔드-프론트 연결 보강 (7.2b 전제조건)

**완료 조건**: 다음 4개가 실측 확인 또는 구현됨 — ① 기간별 활동 집계 API(배포·알림·장애 시계열) ② `inventory-summary`의 ns별 분해 ③ 클러스터/저장소 등록의 진행 이벤트 스트림(연결 위저드 스테이지가 실이벤트 파생 — 데모의 고정 타이머 금지) ④ 알림 이벤트 id 멱등.

- 백엔드 작업열과 API 계약 문서로 합의(HANDOVER §1-6: 이 작업열은 frontend 불가침이므로 계약 요청서를 `docs/api-requests/`에 남기고 진행).

## G4. 서피스 이식 (Master Spec P3~P5 — 데모 견본 매핑)

**완료 조건**: 제품 `/`가 데모 견본과 화면 대 화면으로 동등 이상 — 내비 8항목(D19), 홈 3층(요약 칩+클러스터 카드 v6+위젯 보드 W2~W8, Edit layout=dnd-kit), 리소스 3관점(D18, 흐름=서비스 수준 스코프 의미론), 상세 시트 계약(D3: 인셋·드래그·forceFull·탭5·히스토리·편집 충돌), 알림 단일화(D4: 벨 진행 중/최근, OperationStatusCenter 흡수, 발화 매트릭스 6.2), 연결 모달(D7: 문맥 2곳, 완료가 목록에 실반영 — 견본의 '연결 중' 카드 패턴), AI(D9: FAB·forceFull·action 카드 착지=알림 규칙 탭), 배포/인시던트/타임라인/점검/비용/설정(5.7~5.10 — 견본 `devpreview-surfaces.tsx`가 정답 화면).

- C: 요소 단위 바인딩은 `OPSIA-SURFACE-SPEC.md`(§1~§14)가 유일한 사전. 위젯·게이지 사용처 제한(D21: 게이지 3종, 합성 점수 금지) 준수.
- C: 딥링크는 기존 URL 코덱에 통합(6.3 — 병렬 파서 금지).
- E: 각 서피스 완료 시 견본(5174)과 나란히 띄워 숫자 대조 — 두 화면이 다른 숫자를 말하면 반송.

## G5. 중복·레거시 소거 (결정표 "제거 목록" 전량 집행)

**완료 조건**: 다음이 코드베이스에서 grep 0 — `GitOpsSyncSearch`(D6), `ResourcesCatalog{Mobile,Parts}`(D16), `HelmResourcesDiffView` 자체 diff(D15→UnifiedDiff), 독립 `OperationStatusCenter` 표면(D4), 사이드바 프로필/워크스페이스(D20→헤더), `/workload/*` 고유 UI(D3→시트 래퍼), 병합된 라우트 페이지 셸(`ClustersPage`·`TrafficPage`·`AlertsPage`… → 리다이렉트만), 데모 결함 원장의 "남김" 중 P3~P5 배정분.

- D: 삭제마다 결정표 D# 를 커밋 메시지에 인용. 목록 밖 "이것도 안 쓰는 듯" 삭제는 금지 — E에 검증 요청.

## G6. 프로덕션 폴리싱 + 최종 검증

**완료 조건**: 8.2 판매 품질 스윕 9항목 전부 통과(숫자 교차 대조·중복 UI 0·가짜 컨트롤 0·좌표계·i18n ko/en·모션 토큰/60fps/reduced-motion·접근성(포커스 트랩·aria-live·AA)·다크 모드·1024~1920 뷰포트) + 실 로그인 Playwright E2E(HANDOUT §10-8) + 라이브에서 발표 시나리오(홈→드릴→장애 상세→YAML→AI RCA→규칙 생성→연결) **데모 없이** 완주 녹화.

- E 주관, 전 트랙 결함 반송. 완료 시 P6: `devpreview-*` 빌드 제외(파일은 demo 브랜치에 보존), 데모 브랜치 태그만 남기고 종료 선언.

## G7. 지속 배포 리듬 정착

**완료 조건**: 최근 5회 dev 푸시 배치가 전부 게이트→배포 자동 완주(취소 0), `docs/current-service-state.md`가 최신 digest와 일치.

---

## 보고 형식
각 목표 완료 시 `docs/GOAL-LOG.md`에 1줄: `G# · 완료 · <commit> · <검증 근거 요약>`. 블로커는 `docs/BLOCKERS.md`에 `발견일 · 목표 · 원인 · 해소책 · 상태`.
